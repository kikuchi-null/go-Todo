package main

import "todo/app/controllers"

func main() {

	controllers.StartServer()

	/*
		//user, _ := models.GetUserByID(1)
		todo, _ := models.GetTodo(1)
		task := "Learning Golang and BeeGo"
		//user.CreateTodo(task)
		todo.UpdateTodo(task)
	*/
}
